//Show that hex and octal notations work with long values.
//Use Long.toBinaryString( ) to display the results.

public class H4EX8 {
    public static void main(String[] args)
    {
        long a = 0x2abcl, b = 0420;
        long sum = 0;
        sum = a + b;
        System.out.println("a = " + a + " b = " +b);
        System.out.println("Suma 0x2abc + 0420 = " + sum);
        System.out.println("U dvijkovomu: " + Long.toBinaryString(sum));

    }
}
